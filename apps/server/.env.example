# Local Alchemy (`nub run dev`) and Drizzle. Production uses
# packages/infra/.env.prod instead — never point deploy at this file.
BETTER_AUTH_SECRET=replace-with-a-32-byte-secret
BETTER_AUTH_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3001
