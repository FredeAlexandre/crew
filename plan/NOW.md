# Now

Items are source of truth. This snapshot is for a glance.

## In flight

_(none)_

## Next

Unblocked tasks — claim one, then open a jj workspace.

- `engine-model` — identities, 40-card deck, attempt state (`GAME.md` §3–4, `PRESENTATION.md` §4)
- `view-model` — per-seat projection, visibility, affordances (`PRESENTATION.md` §3.2, §9)

Those two can run in parallel. Keep IDs aligned.

Open milestones (not for claiming): `mil-engine`, `mil-view-model`, `mil-skin`.

## Blocked

**Engine** (after `engine-model`): `engine-trick` and `engine-deal-captain` in parallel, then `engine-tasks` → `engine-comms` → `engine-mission`.

**View / skin:** `view-fixtures` → `skin-playground` and `skin-geometry` in parallel → `skin-motion-sound` and `skin-input`.

**v1:** `v1-table-flow`, `v1-overlays`, `v1-layouts`, `v1-reconnect` wait on the tracks above. Milestone `mil-v1` waits on `mil-engine`, `mil-view-model`, `mil-skin`.

**After v1:** `later-tonoja`, `later-campaign`, `later-illustrated-skin` (milestone `mil-after-v1`).

## Recently done

- `mil-rules-guide` — `GAME.md` (`okttwkrl`)
- `mil-presentation-contract` — `PRESENTATION.md` (`xlnrpqmu`)
