name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: nubjs/setup-nub@v0
      - run: nub ci || (sleep 5 && nub ci) || (sleep 15 && nub ci)
      - run: nub run check
